# Build properties for the service assembly builds
# user.properties.file=../../user.properties
src.dir=src
# This directory is removed when the project is cleaned:
build.dir=build
build.sa.dir=${build.dir}/sa
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.zip=${dist.dir}/testSA.zip

# no.deps=true set the no.deps property if there are no SUs to build
# for each testSU define the properties here.
# dependent SU projects dependency
project.testSU=testSU
project.testSU.build.dir=${build.dir}/testSU/build
project.testSU.dist.dir=${build.dir}/testSU/dist
# dependent SU project's artifacts dependency
reference.testSU.zip=${project.testSU.dist.dir}/testSU.zip

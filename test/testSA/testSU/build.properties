# Build properties for the service unit builds
src.dir=src
# This directory is removed when the project is cleaned:
# build.dir=../../../build/test/testSU/build
build.dir=build
# This directory is removed when the project is cleaned:
# dist.dir=../../../build/test/testSU/dist
dist.dir=dist
dist.zip=${dist.dir}/testSU.zip

